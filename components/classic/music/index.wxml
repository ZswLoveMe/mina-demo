<!-- components/classic/music/index.wxml -->
<view hidden="{{hidden}}" class="classic-wraooer">
    <image src="{{imgSrc}}" />
    <image bind:tap="onplay" src="{{playing?pauseSrc:playSrc}}" />
    <image src="./images/music@tag.png" /> 
    <text>
        {{content}}
    </text>    
</view>